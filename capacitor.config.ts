import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.whitenoiserustoremain2main',
  appName: 'white-noise-rustore-main2-main',
  webDir: 'dist'
};

export default config;
